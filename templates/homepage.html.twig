{% extends 'base.html.twig' %}

{% block headerTitle %}
    E-kebab
{% endblock %}

{% block content %}

    <div class="album py-5 bg-light">
        <div class="container">
        <h2>Compositions viande</h2>
        </br>
          <div class="row">
            
            {% for ekebab in ekebabs %}
                
                <div class="col-md-4">
                <div class="card mb-4 box-shadow">
                    <img class="card-img-top" src="{{ ekebab.imgurl }}" alt="Card image cap">
                    <div class="card-body">
                    <p class="card-text">Viande {{ ekebab.viande }}</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a href="{{ path('product', { 'idProduct': ekebab.id }) }}" class="btn btn-sm btn-outline-secondary">Voir la composition</a>
                            {# <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button> #}
                        </div>
                        <small class="text-muted">{{ ekebab.prix }}€</small>
                    </div>
                    </div>
                </div>
                </div>
            {% endfor %}



        </div>
    </div>

    <ul>
    </ul>
{% endblock %}
